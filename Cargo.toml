[package]
name = "chruldatabase"
version = "0.1.0"
edition = "2021"

[dependencies]
diesel = { version = "2.1.4", features = ["postgres"] }
diesel-derive-enum = { version = "2.1.0", features = ["postgres"] }
rocket = "0.5.0"
